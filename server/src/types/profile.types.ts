export type UpdateProfile = {
  id: string;
  firstName: string;
  lastName: string;
  // userName: string;
  // email: string;
  phoneNumber: number;
  dob: Date;
  location: string;
  about: string;
};
